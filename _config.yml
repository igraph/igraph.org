name: igraph &ndash; the network analysis package
future: true
include: [ .htaccess, .htpasswd, _static, _sources, _images, _downloads, .nojekyll ]
exclude: [ _build, _tools, Gemfile, Gemfile.lock, Makefile, REQUIREMENTS.md, docs, '**/stamp' ]
markdown: kramdown
kramdown:
  input: GFM
url: "https://igraph.org"
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
authors:
  - The igraph Team
defaults:
  -
    scope:
      path: ""
    values:
      layout: "post"
